<h1>Listing notes</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Body</th>
    <th>Group</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @notes.each do |note| %>
  <tr>
    <td><%= note.name %></td>
    <td><%= note.body %></td>
    <td><%= link_to 'Show', group_note_path(@group, note) %></td>
    <td><%= link_to 'Edit', edit_group_note_path(@group, note) %></td>
    <td><%= link_to 'Destroy', group_note_path(@group, note), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Note', new_group_note_path(@group) %>
